apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: activemq-data
  namespace: activemq
spec:
  accessModes:
    - ReadWriteMany  
  resources:
    requests:
      storage: 10Gi
  storageClassName: nfs-client  # 클러스터 NFS StorageClass 이름
